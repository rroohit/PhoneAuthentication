<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PhoneAuthentication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/CarnationPink</item>
        <item name="colorPrimaryVariant">@color/ColumbiaBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/Mauve</item>
        <item name="colorSecondaryVariant">@color/ColumbiaBlue</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/ColumbiaBlue</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PhoneAuthentication.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PhoneAuthentication.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PhoneAuthentication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>